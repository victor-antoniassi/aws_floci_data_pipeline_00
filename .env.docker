# AWS configuration
AWS_REGION=us-east-1

# Local endpoint (all AWS SDKs point here)
ENDPOINT_URL=http://floci:4566

# Docker data directory for persistence
DATA_DIR=./data

# Docker network for container communication
COMPOSE_NETWORK_NAME=floci_network
