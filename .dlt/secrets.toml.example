# dlt secrets example — copy to secrets.toml and adjust
# [destination.filesystem]
# bucket_url = "s3://coingecko-raw"
#
# [destination.filesystem.credentials]
# aws_access_key_id = "test"
# aws_secret_access_key = "test"
